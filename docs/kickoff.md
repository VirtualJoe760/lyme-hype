# Kickoff prompt

Paste this as the first message in a fresh session, opened in this repo
(`F:\web-clients\joseph-sardella\lyme-hype\`). `AGENTS.md` loads automatically via
`CLAUDE.md`. Overwritten each round rather than accumulated — `history.md` is where
past rounds' narrative lives, not this file.

---

This is Lyme Hype. Read `docs/README.md`, then `docs/build-plan.md`.

What needs to be done: everything in `build-plan.md` still open — Phases 10 through 13
(multitrack timeline, Scripting panel, resizable/collapsible panels, the Create panel
and its Motion graphics workflow), the OpenAI image connector and connector-tier
routing left over from Phase 4, and anything else the docs flag as unfinished. The
specs in `docs/ui/` and `docs/connectors/` are complete design docs, not sketches —
build to them, and update them wherever you make a call they leave to you or find
something they got wrong.

Full autonomy. Go as far as the real work allows.

Go.
